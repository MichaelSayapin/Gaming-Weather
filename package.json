{
  "name": "weather_heartbeat",
  "version": "1.0.0",
  "description": "Reads weather info and writes to InfluxDB",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Sayapin",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.553.0",
    "commander": "^3.0.2",
    "influx": "^5.0.7",
    "request": "^2.88.0",
    "request-promise": "^4.2.4",
    "unix-timestamp": "^0.2.0"
  }
}
